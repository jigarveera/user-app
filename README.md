# Deploying my express server app on github
# Integrating the docker-compose.yaml file to github actions
# Automating Docker image building and pushing the latest changes to docker hub with github actions